post:
  description: "Переключает камеру в ночной режим работы"
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: string
                description: Статус выполнения операции
          example:
            status: "ok"
  summary: Включить ночной режим
  tags:
  - Camera